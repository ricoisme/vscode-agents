# Fix SRT Enhanced - Python Dependencies
# 安裝指令: pip install -r requirements.txt

# 核心套件（強烈建議安裝，以獲得最佳繁體中文修正效果）
opencc-python-reimplemented>=0.1.7  # 簡繁轉換
pycorrector>=1.0.0                   # 中文錯別字修正
pyspellchecker>=0.8.0                # 英文拼字檢查（本地字典，快速）

# 可選套件（進階語法檢查，會下載語言模型約 100-200MB，首次較慢）
# language-tool-python>=2.8.0       # 深度語法檢查（中英文）

# Python 版本需求: >= 3.8
